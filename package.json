{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "version": "1.0.0",
  "main": "index.js",
  "author": "Maxime Bret <bret.maxime@gmail.com>",
  "license": "MIT",
  "scripts": {
    "start": "lerna exec --parallel -- yarn start",
    "publish-packages": "yarn tsc && lerna publish",
    "build-core": "cd packages/reader && yarn build && cd ../streamer && yarn build && cd ../react && yarn build && cd ../reader-enhancer-bookmarks && yarn build",
    "tsc": "lerna exec --parallel -- yarn tsc",
    "lint": "lerna exec --parallel -- yarn lint",
    "misc:check-pack": "npm pack && tar -xvzf *.tgz && rm -rf package *.tgz"
  },
  "workspaces": [
    "packages/*"
  ]
}
