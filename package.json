{
  "name": "@prose-reader/root",
  "private": true,
  "version": "1.0.0",
  "main": "index.js",
  "author": "Maxime Bret <bret.maxime@gmail.com>",
  "license": "MIT",
  "scripts": {
    "start": "lerna exec --parallel -- yarn start",
    "publish-packages": "yarn test && yarn build:prod && yarn tsc && lerna publish",
    "build": "lerna run build",
    "build:prod": "lerna run build:prod",
    "tsc": "lerna exec --parallel -- yarn tsc",
    "lint": "lerna exec --parallel -- yarn lint",
    "test": "lerna exec --parallel -- yarn test",
    "misc:check-pack": "npm pack && tar -xvzf *.tgz && rm -rf package *.tgz",
    "dev:link-all": "./scripts/link-all.sh"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/react": "*",
    "@types/react-dom": "*",
    "lerna": "^4.0.0",
    "rxjs": "*",
    "webpack": "^5.64.3"
  },
  "resolutions": {
    "@types/react": "17.0.37",
    "typescript": "4.5.4",
    "rxjs": "7.5.1"
  }
}
