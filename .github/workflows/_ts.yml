name: ts

on:
  workflow_call:

jobs:
  ts:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup
        with:
          build: all
      - name: Check
        run: pnpm run tsc
