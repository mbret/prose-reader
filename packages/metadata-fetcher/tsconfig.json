{
  "extends": "../../tsconfig.node.json",
  "compilerOptions": {
    // `report.ts` reads the package name from package.json for its namespace
    "resolveJsonModule": true
  },
  "include": ["src", "vite.config.ts"]
}
