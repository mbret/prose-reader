{
  "name": "prose-reader-metadata-fetcher-api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "node --conditions=prose-source --watch --watch-preserve-output src/server.ts",
    "serve": "node src/server.ts",
    "tsc": "tsc",
    "test": "vitest run",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down"
  },
  "dependencies": {
    "@prose-reader/metadata-fetcher": "^1.335.0",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@prose-reader/archive-reader": "^1.335.0",
    "@types/express": "^5.0.6",
    "@zip.js/zip.js": "^2.8.33",
    "typescript": "*"
  }
}
